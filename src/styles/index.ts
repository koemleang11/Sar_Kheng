import {StyleSheet} from 'react-native';

const defaultStyle = {
  marginHorizontal: 10,
  pandding: 10,
  margin: 10,
  borderRadius: 10,
  borderBottomWidth: 0.5,
};

export const makerStyle = StyleSheet.create({
  AliginCenter: {
    justifyContent: 'center',
    alignContent: 'center',
  },
});

export default defaultStyle;
